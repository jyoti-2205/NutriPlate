# Local development (npm start) — uses localhost via api.js default
# REACT_APP_API_URL=http://localhost:5000/api

# Production / Vercel build — also set in Vercel dashboard if needed
REACT_APP_API_URL=https://nutriplate-production.up.railway.app/api
